<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form view - Chat interface -->
    <record id="view_hr_ai_assistant_form" model="ir.ui.view">
        <field name="name">hr.ai.assistant.form</field>
        <field name="model">hr.ai.assistant</field>
        <field name="arch" type="xml">
            <form string="HR AI Assistant">
                <header>
                    <button name="action_ask_ai" string="🚀 Hỏi AI" type="object" class="oe_highlight"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>🤖 HR AI Assistant</h1>
                        <h3>Hỏi tôi bất cứ điều gì về nhân viên!</h3>
                    </div>
                    
                    <group>
                        <field name="question" placeholder="Ví dụ: Danh sách nhân viên? Lương của Nguyễn Văn A?" widget="text"/>
                    </group>
                    
                    <notebook>
                        <page string="📋 Câu trả lời" name="answer">
                            <field name="answer" widget="html" nolabel="1"/>
                        </page>
                        <page string="ℹ️ Chi tiết" name="info">
                            <group>
                                <field name="user_id"/>
                                <field name="employee_id"/>
                                <field name="query_time"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tree view - History -->
    <record id="view_hr_ai_assistant_tree" model="ir.ui.view">
        <field name="name">hr.ai.assistant.tree</field>
        <field name="model">hr.ai.assistant</field>
        <field name="arch" type="xml">
            <tree string="Lịch sử hỏi đáp">
                <field name="query_time"/>
                <field name="user_id"/>
                <field name="question"/>
                <field name="employee_id"/>
            </tree>
        </field>
    </record>

    <!-- Search view -->
    <record id="view_hr_ai_assistant_search" model="ir.ui.view">
        <field name="name">hr.ai.assistant.search</field>
        <field name="model">hr.ai.assistant</field>
        <field name="arch" type="xml">
            <search>
                <field name="question"/>
                <field name="employee_id"/>
                <field name="user_id"/>
                <filter string="Hôm nay" name="today" domain="[('query_time', '&gt;=', datetime.datetime.now().replace(hour=0, minute=0, second=0))]"/>
                <filter string="Tuần này" name="this_week" domain="[('query_time', '&gt;=', (datetime.datetime.now() - datetime.timedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <group expand="0" string="Nhóm theo">
                    <filter string="Người hỏi" name="group_user" context="{'group_by': 'user_id'}"/>
                    <filter string="Nhân viên" name="group_employee" context="{'group_by': 'employee_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hr_ai_assistant" model="ir.actions.act_window">
        <field name="name">HR AI Assistant</field>
        <field name="res_model">hr.ai.assistant</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_today': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                🤖 Chào mừng đến với HR AI Assistant!
            </p>
            <p>
                Hỏi tôi về thông tin nhân viên:<br/>
                • "Lương tháng này của Nguyễn Văn A"<br/>
                • "Số ngày nghỉ của NV001 tháng 1"<br/>
                • "Nhân viên nào đi muộn nhiều nhất"<br/>
            </p>
        </field>
    </record>

    <!-- Quick ask action - Simple Form -->
    <record id="action_hr_ai_quick_ask" model="ir.actions.act_window">
        <field name="name">💬 Hỏi AI nhanh</field>
        <field name="res_model">hr.ai.assistant</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_question': ''}</field>
    </record>

    <!-- Simple Ask Form -->
    <record id="view_hr_ai_quick_ask_form" model="ir.ui.view">
        <field name="name">hr.ai.quick.ask.form</field>
        <field name="model">hr.ai.assistant</field>
        <field name="arch" type="xml">
            <form string="Hỏi AI">
                <header>
                    <button name="action_ask_ai" string="🚀 Hỏi AI" type="object" class="oe_highlight"/>
                </header>
                <sheet>
                    <group>
                        <field name="question" placeholder="Ví dụ: Danh sách nhân viên? Lương của Nguyễn Văn A?" nolabel="1"/>
                    </group>
                    <notebook>
                        <page string="📋 Câu trả lời">
                            <field name="answer" widget="html" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
